# Database Configuration
# MongoDB connection string for the UAE Ecosystem Bot database
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=YourApp

# AI Provider API Keys
# Multiple API keys can be provided for each provider using comma-separated format
# The system will rotate through keys using round-robin strategy to handle rate limits

# OpenRouter API Keys
# GET YOUR KEY HERE: https://openrouter.ai/keys
# Supports free models: arcee-ai/trinity-large-preview:free, google/gemma-3-4b-it:free
# Format: key1,key2,key3 (comma-separated, no spaces)
OPENROUTER_API_KEYS=sk-or-v1-your-key-here,sk-or-v1-another-key-here

# Cerebras API Keys
# GET YOUR KEY HERE: https://cloud.cerebras.ai/
# Supports model: gpt-oss-120b (ultra-fast inference, typically ~150ms response time)
# Format: key1,key2,key3 (comma-separated, no spaces)
CEREBRAS_API_KEYS=csk-your-key-here,csk-another-key-here

# Moonshot AI API Keys
# GET YOUR KEY HERE: https://platform.moonshot.cn/console/api-keys
# Supports models: kimi-k2-0905-preview, moonshot-v1-32k
# Format: key1,key2,key3 (comma-separated, no spaces)
MOONSHOT_API_KEYS=sk-your-moonshot-key-here,sk-another-moonshot-key-here

# Notes:
# - All API key variables support multiple keys in comma-separated format
# - The system uses round-robin rotation to distribute requests across keys
# - When a key hits rate limits, it's temporarily removed from rotation
# - If all keys for a provider are exhausted, the system falls back to the next provider
# - Provider fallback order: OpenRouter -> Cerebras -> Moonshot
# - If all providers fail, organizations are saved without AI classification
